一、选用PHP实现了MyGreeter类
运行方式为：进入项目根目录，
```
cd php
make dev-tests
```

二、
修复Dockerfile（安装make、curl）
make dev-tests报错
```
docker-compose exec recruit make tests
OCI runtime exec failed: exec failed: unable to start container process: exec: "make": executable file not found in $PATH: unknown
make: *** [dev-tests] Error 126
```
原因是php8.3容器中没有安装make命令
需要修改一下Dockerfile:
```
FROM php:8.3-alpine
RUN apk add --no-cache make
```
然后重新构建镜像：
```
docker-compose build
make dev-tests
```
三、 修复MyGreeterTest
测试类存在以下问题：
    1. 断言太弱：test_greeting只检查返回值长度>0,没有考虑边界值测试
    2. 依赖真实系统时间，不可控、不可重复
改进方法：
    1. 增加边界值测试
    2. 让greeting()接受一个可选的时间参数